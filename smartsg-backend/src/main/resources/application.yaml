spring:
  application:
    name: smartsg-backend

  datasource:
    url: jdbc:postgresql://cs5224-smartsg.c3kcyoksqwbl.ap-southeast-1.rds.amazonaws.com:5432/postgres?ssl=true&sslmode=require
    username: postgres
    password: cs5224_project
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

# jwt token expiration time(log off time)
jwt:
  secret: smartsg-cs5224-jwt-secret-key-2024-this-needs-to-be-at-least-256-bits-long
  expiration: 86400000 # 24 hours in milliseconds
